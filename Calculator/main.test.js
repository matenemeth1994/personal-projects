import { calculator, updateDisplay } from "./main";

// describe('calculator', () => {
// })
        test('calculator object should exist', () => {
            expect(typeof calculator).toEqual('Object')
        });